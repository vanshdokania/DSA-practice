#include <bits/stdc++.h>
using namespace std;
// binary string , integer k, eg. 00110110, k = 2
// true if every binary code of size k in the string
int main() {

    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    #ifndef ONLINE_JUDGE
    freopen("input.txt", "r", stdin);
    freopen("output.txt", "w", stdout);
    #endif

    
    return 0;

}